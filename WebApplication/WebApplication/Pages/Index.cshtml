@page 
@*@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers*@
@model IndexModel   // Lưu ý là nếu như sử dụng model thì functions block sẽ không có ý nghĩa
@{
    ViewData["Title"] = "Home page";
}

@*sử dụng functions code block để get post*@
@*<h3>@Message</h3>
    <form method="post">
        <p><button class="btn btn-primary" type="submit">Send Post Request</button></p>
    </form>
    <p><a href="/" class="btn btn-primary">Send GET request</a></p>*@

@*@functions{
        public string Message { get; set; }
        public void OnGet()
        {
            Message = "This is a GET request";
        }
        public void OnPost()
        {
            Message = "This is a POST request";
        }
    }*@


@*Sử dụng model class để get post*@

<h3>@Model.Message</h3>
<form method="post">
    <p><button class="btn btn-primary" type="submit">Send Post Request</button></p>
</form>
<p><a href="/" class="btn btn-primary">Send GET request</a></p>
